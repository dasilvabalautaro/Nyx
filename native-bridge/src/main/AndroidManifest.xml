<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <!-- Solo para el descubrimiento mDNS en LAN (pruebas); WAN usa DHT + rendezvous. -->
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />

    <!-- El Foreground Service (NyxForegroundService) vive en :app, que es quien puede
         inyectar ChatService (:p2p-signaling); este módulo no lo ve por el grafo de deps. -->
    <application />

</manifest>
